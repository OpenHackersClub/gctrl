<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>dev.gctrl.kernel</string>

    <key>ProgramArguments</key>
    <array>
        <string>__GCTRL_BIN__</string>
        <string>serve</string>
        <string>--host</string>
        <string>__GCTRL_HOST__</string>
        <string>--port</string>
        <string>__GCTRL_PORT__</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
        <key>Crashed</key>
        <true/>
    </dict>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>__LOG_DIR__/kernel.out.log</string>

    <key>StandardErrorPath</key>
    <string>__LOG_DIR__/kernel.err.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>RUST_LOG</key>
        <string>info</string>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        <!-- Driver secrets + app config — populated from ~/.config/gctrl/env
             by install-launchd.sh. Empty placeholders here are intentional;
             the daemon tolerates missing env (drivers refuse to send when
             their secret is unset, with a clear error). -->
        <key>TELEGRAM_BOT_TOKEN</key>
        <string>__TELEGRAM_BOT_TOKEN__</string>
        <key>TELEGRAM_PRIMARY_CHAT_ID</key>
        <string>__TELEGRAM_PRIMARY_CHAT_ID__</string>
        <key>DISCORD_NOTIFY_WEBHOOK_URL</key>
        <string>__DISCORD_NOTIFY_WEBHOOK_URL__</string>
        <key>UBER_VAULT_DIR</key>
        <string>__UBER_VAULT_DIR__</string>
        <!-- Scheduler exec target — operator opt-in. Set to "1" + an
             allowlist of absolute bin paths in ~/.config/gctrl/env to enable
             vault-defined cron schedules that exec the uber CLI. -->
        <key>GCTRL_SCHEDULER_EXEC_ENABLED</key>
        <string>__GCTRL_SCHEDULER_EXEC_ENABLED__</string>
        <key>GCTRL_SCHEDULER_EXEC_ALLOWED_PROGRAMS</key>
        <string>__GCTRL_SCHEDULER_EXEC_ALLOWED_PROGRAMS__</string>
    </dict>

    <key>ProcessType</key>
    <string>Background</string>
</dict>
</plist>
