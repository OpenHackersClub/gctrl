/target
.tmp/
*.duckdb
*.duckdb.wal
node_modules/
dist/
dist-web/

# Test artifacts
playwright-report/
test-results/

.obsidian
.DS_Store
.nx/
**/.wrangler/

# dotenvx — encrypted .env is safe to commit, plaintext and keys are not
.env
.env.*
!.env.example
!.env.vault
.env.keys

# App vaults — operational data (BOARD issues, INBOX items, agent comments)
# lives in an external vault dir (Obsidian-mountable, not git-tracked).
# Configure via GCTRL_BOARD_DIR. apps/gctrl-board/vault/specs/ stays in repo.
apps/gctrl-board/vault/BOARD/
apps/gctrl-inbox/vault/INBOX/
